const styles = (theme) => ({
    root: {
      background: "LightBlue",
      display: "flex",
      justifyContent:"center",
      alignItems: "center"
    },
    landingIcon: {
      position: 'absolute',
      top: '85vh',
      color: 'white',
      zIndex: 1,
    },
  });

export default styles;
