const styles = (theme) => ({
    root: {
      position: 'fixed',
      height: '100vh',
      minHeight: '100vh',
      width: '100vw',
      minWidth: '100vw',
      display: 'flex',
      textAlign: 'center',
      alignItems: 'center',
      flexDirection: 'column',
      zIndex: 1,
    },
  });

export default styles;
