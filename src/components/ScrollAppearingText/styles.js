const styles = (theme) => ({
    root: {
      backgroundColor: '#282c34',
    },
    landingIcon: {
      position: 'absolute',
      bottom: '1px',
      color: 'white',
      zIndex: 2,
      backgroundColor: '#282c34',
    },

  });

export default styles;
